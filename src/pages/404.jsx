import React from 'react';
import { Typography } from "@mui/material"

const NotFound = () => {
    return(
        <Typography variant="h4" sx={{ textAlign: 'center' }}>페이지가 존재하지 않습니다.</Typography>
    )
}

export default NotFound